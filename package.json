{
  "name": "color-coded-clients",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that colors each Business Central environment.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "icons": "powershell -ExecutionPolicy Bypass -File tools/make-icons.ps1",
    "package": "powershell -ExecutionPolicy Bypass -File tools/package.ps1"
  }
}
